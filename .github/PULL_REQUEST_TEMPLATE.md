## Summary

<!-- What does this change and why? -->

## Checklist

- [ ] `uv run pytest` passes locally
- [ ] `uv run pre-commit run --all-files` passes
- [ ] New clocks include a notebook in `clocks/notebooks/` and a validated entry in `clocks/metadata/clock_metadata.json`
- [ ] Scientific behavior of existing clocks is unchanged
